# ローカルで動作確認済みのバージョンに固定する。
# 以前は >=1.6.0 と上限なしだったため、2026-07-28 リリースの 2.0.0 が
# 自動的に入り、mcp.server.fastmcp が削除されていて起動に失敗した。
# 上げるときは server.py の import が通ることを確認してから上げること。
mcp[cli]==1.27.0
requests>=2.31.0
beautifulsoup4>=4.12.0
PyYAML>=6.0.1
uvicorn>=0.30.0
fastapi>=0.110.0
lxml>=5.0.0
gspread>=6.0.0
google-auth>=2.0.0
